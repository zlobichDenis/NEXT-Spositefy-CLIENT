import { styled, Grid, Box } from '@mui/material';

export const TrackListContainer = styled(Grid)`
  flex-direction: column;
`;

export const TrackListBox = styled(Box)`
  padding: 20px;
  display: flex;
  flex-direction: column;
  gap: 15px;
`;