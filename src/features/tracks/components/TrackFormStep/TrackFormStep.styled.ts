import { Card, Grid, styled } from "@mui/material";

export const TrackFormStepsWrapper = styled(Grid)`
  margin: 70px 0;
  height: 270px;
  justify-content: center;
`;

export const TrackFormStepCard = styled(Card)`
  margin: 70px 0;
  width: 600px;
  justify-content: center;
`